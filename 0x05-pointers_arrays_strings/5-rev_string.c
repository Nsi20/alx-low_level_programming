#include "main.h"

/**
 * rev_string - prints reversed string
 * @s: pointer
 * Return: void
 */
void rev_string(char *s)
{
	char tmp;
	int i, lenght1, lenght2;

	lenght1 = 0;
	lenght2 = 0;

	while (s[length1] != '\0')
		lenght1++;

	lenght2 = lenght1 - 1
		for (i = 0; i < lenght1 / 2; i++)
		{
			"samalx" "xlamas"
				tem = s[i];
			s[i] = s[length2];
			s[lenght2] = tmp;
			length2 -= 1;
		}
}
